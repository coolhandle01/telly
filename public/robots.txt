# telly — a television built from your own YouTube subscriptions.
# https://github.com/coolhandle01/telly

User-agent: *
Allow: /

# There is nothing here to keep out of an index. The site is a static bundle
# with no server and no central database; every request it makes is the visitor's own,
# to Google, carrying the visitor's own token, which never leaves their tab.
